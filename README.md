# User Manager

> 🚧 This project is currently under development. 🚧

This is a simple CRUD (Create, Read, Update, Delete) application built with React. It demonstrates how to manage users with basic functionalities, like adding, viewing, editing, and deleting users. The app uses \`useState\`, \`useEffect\`, \`React Router DOM\` for navigation, \`fetch\` to make API calls, and \`CSS Modules\` for styling.

## Technologies Used

- **React**: A JavaScript library for building user interfaces.
- **React Router DOM**: For routing and navigation between different pages.
- **useState**: For managing state in functional components.
- **useEffect**: For handling side effects and fetching data.
- **fetch API**: To make HTTP requests to interact with the API.
- **CSS Modules**: For modular, scoped, and reusable styling.

## Features

- **Create a User**: You can add new users by filling out a form and submitting the data.
- **List Users**: Displays all the users stored in the system.
- **Edit a User**: Allows you to edit a user's information (name, email, phone).
- **Delete a User**: Enables you to delete users from the list.
- **Responsive Design**: The application adjusts to different screen sizes, ensuring a great experience on both desktop and mobile.

## Getting Started

### Prerequisites

- npm or yarn

### Installation

1. Clone this repository:

   ```bash
    git clone https://github.com/diasmurillo/UserManager.git
    cd simple-crud-app
    npm install
    npm run dev

